name: LEGACY — Gerar 28 audios N2 do livro

# Workflow congelado após adoção do N3 Natural em 31/08/2026.
# Mantido somente para rollback deliberado. Não reage mais a push/payload.
on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  legacy-info:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "N2 congelado. Runtime canônico: N3 Natural."
          echo "Use o histórico Git/audio/n2 apenas para comparação ou rollback explícito."
